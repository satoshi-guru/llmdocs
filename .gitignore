output/
audits/
__pycache__/
.pytest_cache/
*.py[cod]
.venv/
venv/
*.egg-info/
dist/
build/
.DS_Store

# Local Claude Code config — ship settings.json.example instead
.claude/settings.json

# internal pre-publish handoff (private identity context) — never publish
PUBLISH_TODO.md
FINAL_PLAN.md

# stale internal sprint plan — superseded by FINAL_PLAN.md (never publish)
NEXT_STEPS.md

# internal session handoff notes (private identity/strategy context) — never publish
SESSION_HANDOFF*.md
